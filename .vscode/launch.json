{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch TypeScript",
            "type": "node",
            "request": "launch",
            "program": "${workspaceRoot}/src/index.ts",
            "sourceMaps": true,
            "outDir": "${workspaceRoot}/build",
            "cwd": "${workspaceRoot}"
        }
    ]
}