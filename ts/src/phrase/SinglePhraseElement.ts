import TokenNode from '../TokenNode';
import PhraseElementBase from './PhraseElementBase';

export abstract class SinglePhraseElement extends PhraseElementBase {
  connectToNodes(input:TokenNode[]):TokenNode[] {
    var output = [];


    return output;
  }
}
